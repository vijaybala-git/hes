mesa==3.5.1
solara==1.57.4
starlette==0.52.1
networkx>=3.0
numpy>=2.0
pandas>=2.0
matplotlib>=3.9
plotly>=6.0
anywidget>=0.9

# Offline rate-projection sub-project (Jupyter analysis; not imported by the live app)
jupyterlab>=4.0
pyxlsb>=1.0      # reads the .xlsb ACC electric workbook
openpyxl>=3.1    # reads the .xlsx ACC gas workbook
