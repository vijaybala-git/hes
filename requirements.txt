mesa==3.5.1
solara==1.57.4
# Pin the whole vuetify-2 UI stack. solara-ui 1.57.4 declares these UNBOUNDED
# (ipyvuetify>=1.6.10, ipyvue>=1.9.0, reacton>=1.9), so a fresh HF build otherwise pulls
# ipyvuetify/ipyvue 2.x (vuetify 3) — which renames ExpansionPanelHeader (crashes
# solara.Details), hides the header buttons, and restyles the sliders. These are the
# versions the app is developed and tested against locally.
ipyvuetify==1.11.3
ipyvue==1.12.0
reacton==1.9.1
ipywidgets==8.1.8
starlette==0.52.1
networkx>=3.0
numpy>=2.0
pandas>=2.0
matplotlib>=3.9
plotly>=6.0
anywidget>=0.9

# Offline rate-projection sub-project (Jupyter analysis; not imported by the live app)
jupyterlab>=4.0
pyxlsb>=1.0      # reads the .xlsb ACC electric workbook
openpyxl>=3.1    # reads the .xlsx ACC gas workbook
